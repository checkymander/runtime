name: Build .NET

on:
  push:
    branches:
      - release/8.0
  pull_request:
    branches:
      - main

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      # Checkout code
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Build Docker Image
        run: docker build -t custom-dotnet .

      # Run Docker container and execute commands
      - name: Run .NET build inside of Docker
        run: docker run --name custom-build custom-dotnet

      # Extract artifacts from the container
      - name: Copy Artifacts
        run: |
          docker cp custom-build:/opt/dotnet/artifacts ./artifacts
          docker stop custom-build
          docker rm custom-build

      # Upload artifacts
      - name: Upload Artifacts
        uses: actions/upload-artifact@v3
        with:
          name: build-artifacts
          path: ./artifacts
